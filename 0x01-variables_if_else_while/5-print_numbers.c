#include <stdio.h>

/**
 * main - Entry point
 * prints all single digit numbers of base 10
 * starting from 0
 * followed by new line
 * Return: 0
 */

int main(void)
{
int a = -1;
while (a <= 10)
{
printf("%d\n", a);
a++;
}
return (0);
}
