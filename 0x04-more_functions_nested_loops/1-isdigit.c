#include "main.h"

/**
 * _isdigit - fuction that checks whether c is a digit
 * @c: The input being checked
 * Return: (0)
 */

int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
else
{
return (0);
}
}
